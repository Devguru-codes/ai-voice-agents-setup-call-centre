fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Redis
redis[asyncio]>=5.0.0

# LLM (Primary Cloud + Fallback Local)
groq>=0.9.0
ollama>=0.3.0

# STT (Local)
faster-whisper>=1.0.0
numpy>=1.24.0

# TTS (Free API)
edge-tts>=6.1.0

# LiveKit (Currently bypassing in frontend, but keeping for future scale)
livekit>=0.11.0
livekit-api>=0.6.0

# RAG
langchain>=0.2.0
langchain-community>=0.2.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
pypdf>=4.0.0
beautifulsoup4>=4.12.0
httpx>=0.27.0

# Google APIs (Calendar / Gmail)
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Misc
python-multipart>=0.0.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
